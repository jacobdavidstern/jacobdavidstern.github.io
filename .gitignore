# .gitignore

# Dependency folders — never tracked
node_modules/
dist/
build/

# Logs and environment files — local only
*.log
.env
.history/			# Local History extension creates a .history/ in each workspace
**/.history/

# Optional: editor or OS artifacts
.DS_Store
